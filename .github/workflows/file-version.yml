# name: Write version to file

# on:
#   push:
#     branches: [ dev ]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Get branch names
#         id: branch-current
#         uses: tj-actions/branch-names@v8

#       - name: Get latest tag
#         id: tag-latest
#         uses: JinoArch/get-latest-tag@latest

#       - name: Write in VERSION file
#         run: |
#             echo "${{ steps.tag-latest.outputs.latestTag }}" > VERSION

          